# Plantilla de variables de entorno — Tesis PR-196
#
# CÓMO USARLA:
#   1) Copia este archivo a `.env`            ->  cp .env.example .env
#   2) Pega tu API key de Anthropic abajo (reemplaza el placeholder).
#
# El archivo `.env` está en .gitignore, así que tu key NUNCA se sube a GitHub.
# La política LLM (src/tesis/politicas/llm.py) lo lee automáticamente.

ANTHROPIC_API_KEY=pega-tu-key-aqui
